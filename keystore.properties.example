# Copy to keystore.properties in the repo root (gitignored).
# Or set ONIONPEEL_KEYSTORE, ONIONPEEL_KEYSTORE_PASSWORD, ONIONPEEL_KEY_ALIAS, ONIONPEEL_KEY_PASSWORD env vars.

storeFile=onionpeel-release.jks
storePassword=your-store-password
keyAlias=onionpeel
keyPassword=your-key-password
