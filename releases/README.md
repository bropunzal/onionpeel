Beta APKs are built with `.\scripts\build-beta.ps1` and copied here as `onionpeel-<version>.apk` (e.g. `onionpeel-0.2.0-beta.1.apk`).

APK files are gitignored — attach them to [GitHub Releases](https://github.com/bropunzal/dumbphone/releases) for testers.
